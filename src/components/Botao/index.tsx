import React from 'react';

class Botao extends React.Component {
    render() {
        return (
            <button>
                aqui esta o botao mesmo?
            </button>
        )
    }
}

export default Botao;